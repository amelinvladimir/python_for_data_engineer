import numpy as np

# Для Python list * 2 означает повторение списка.
prices = [100, 200, 300]
print(prices * 2)

# Для NumPy * 2 означает умножить каждый элемент на 2.
prices = np.array([100, 200, 300])
print(prices * 2)
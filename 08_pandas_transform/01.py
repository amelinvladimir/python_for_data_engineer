# Зачем вообще преобразовывать данные

import pandas as pd

orders = pd.DataFrame({
    "id": [1001, 1002, 1003],
    "price": ["1200", "3500", "800"],
    "qty": [2, 1, 5],
    "status": ["C", "C", "R"]
})

print(orders)

# Что здесь неудобно для аналитика?

# price — строка;
# status — непонятные коды;
# qty — не очень говорящее название;
# нет выручки;
# нет признака возврата.